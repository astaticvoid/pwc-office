{
  "name": "pwc-office-tests",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "vitest": "^1.0.0"
  },
  "scripts": {
    "test":           "vitest run",
    "test:web":       "playwright test",
    "test:web:local": "BASE_URL=http://localhost:8081 playwright test"
  }
}
